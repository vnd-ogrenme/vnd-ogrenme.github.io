const $ = (s, root=document) => root.querySelector(s);
const $$ = (s, root=document) => [...root.querySelectorAll(s)];
const cache = {};
async function loadJSON(path){ if(cache[path]) return cache[path]; const r=await fetch(path); if(!r.ok) throw new Error(path); return cache[path]=await r.json(); }
function esc(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
function header(active=''){
  return `<header class="site-header"><div class="container nav"><a class="brand" href="./index.html"><span class="brand-mark">VND</span><span>VND Öğrenme<small>TÜBİTAK 1001</small></span></a><button class="menu-button" aria-label="Menüyü aç">Menü</button><nav class="nav-links"><a class="${active==='home'?'active':''}" href="./index.html">Proje</a><a class="${active==='news'?'active':''}" href="./haberler.html">Haberler</a><a class="${active==='history'?'active':''}" href="./gecmis.html">Geçmiş</a><a class="${active==='pubs'?'active':''}" href="./yayinlar.html">Yayınlar</a><a class="${active==='results'?'active':''}" href="./sonuclar.html">Sonuçlar</a><a class="nav-cta" href="https://github.com/vnd-ogrenme" target="_blank" rel="noopener">GitHub ↗</a></nav></div></header>`;
}
function footer(){return `<footer class="footer"><div class="container footer-grid"><div><strong>VND Öğrenme</strong><p>Yeni bir vuru üreten nöral devre tabanlı öngörüsel kodlama yapısı ve öğrenme kuralının geliştirilmesi.</p></div><div class="footer-links"><a href="./haberler.html">Haberler</a><a href="./gecmis.html">Proje geçmişi</a><a href="./yayinlar.html">Yayınlar</a><a href="./sonuclar.html">Sonuçlar</a><a href="https://github.com/vnd-ogrenme">GitHub</a></div></div></footer>`}
function mountChrome(active){$('#site-header').innerHTML=header(active);$('#site-footer').innerHTML=footer();$('.menu-button').addEventListener('click',()=>$('.nav-links').classList.toggle('open'));}
function newsCard(n, featured=false){return `<a class="news-card ${featured?'featured':''}" href="${esc(n.url||'#')}"><div class="news-media">${n.image?`<img src="${esc(n.image)}" alt="" onerror="this.remove()">`:''}</div><div class="news-body"><span class="tag">${esc(n.type)}</span><span class="news-date">${esc(n.displayDate)}</span><h3>${esc(n.title)}</h3><p>${esc(n.summary)}</p></div></a>`}
async function initHome(){mountChrome('home');const [p,news,pubs]=await Promise.all([loadJSON('./data/project.json'),loadJSON('./data/news.json'),loadJSON('./data/publications.json')]);$('#hero-title').innerHTML=`${esc(p.title).replace('Öngörüsel Kodlama','<span>Öngörüsel Kodlama</span>')}`;$('#hero-desc').textContent=p.description;$('#project-status').textContent=p.status;$('#project-start').textContent=p.startDate;$('#overview-copy').innerHTML=p.overview.map(x=>`<p>${esc(x)}</p>`).join('');$('#objectives').innerHTML=p.objectives.map(x=>`<div class="objective">${esc(x)}</div>`).join('');const sortedNews=[...news].sort((a,b)=>b.date.localeCompare(a.date));$('#news-grid').innerHTML=sortedNews.slice(0,4).map((n,i)=>newsCard(n,i<2)).join('');$('#publication-count').textContent=pubs.length;$('#news-count').textContent=news.length;}

async function initNews(){mountChrome('news');const news=await loadJSON('./data/news.json');const sorted=[...news].sort((a,b)=>b.date.localeCompare(a.date));const types=[...new Set(sorted.map(x=>x.type))];$('#news-filters').innerHTML=['Tümü',...types].map((t,i)=>`<button class="filter ${i===0?'active':''}" data-type="${t==='Tümü'?'all':esc(t)}">${esc(t)}</button>`).join('');renderNews(sorted);$$('.filter').forEach(b=>b.addEventListener('click',()=>{$$('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');renderNews(b.dataset.type==='all'?sorted:sorted.filter(x=>x.type===b.dataset.type));}));}
function renderNews(items){$('#all-news').innerHTML=items.map(n=>newsCard(n,false)).join('') || '<div class="empty">Henüz haber yok.</div>'}

async function initHistory(){mountChrome('history');const items=await loadJSON('./data/timeline.json');const years=[...new Set(items.map(x=>x.year))].sort().reverse();$('#history-filters').innerHTML=['Tümü',...years].map((y,i)=>`<button class="filter ${i===0?'active':''}" data-year="${y==='Tümü'?'all':y}">${y}</button>`).join('');renderTimeline(items);$$('.filter').forEach(b=>b.addEventListener('click',()=>{$$('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');renderTimeline(b.dataset.year==='all'?items:items.filter(x=>x.year===b.dataset.year));}));}
function renderTimeline(items){$('#timeline').innerHTML=[...items].sort((a,b)=>b.date.localeCompare(a.date)).map(x=>`<article class="timeline-item" id="${esc(x.id)}"><div class="timeline-date">${esc(x.displayDate)}</div><div class="timeline-card"><span class="tag">${esc(x.category)}</span><h3>${esc(x.title)}</h3><p>${esc(x.description)}</p>${x.meta?.length?`<div class="timeline-meta">${x.meta.map(m=>`<span>${esc(m)}</span>`).join('')}</div>`:''}${x.items?.length?`<ul>${x.items.map(i=>`<li>${esc(i)}</li>`).join('')}</ul>`:''}${x.links?.length?`<div class="timeline-meta">${x.links.map(l=>`<a href="${esc(l.url)}" target="_blank" rel="noopener">${esc(l.label)} ↗</a>`).join('')}</div>`:''}</div></article>`).join('')}
async function initPublications(){mountChrome('pubs');const pubs=await loadJSON('./data/publications.json');const types=[...new Set(pubs.map(x=>x.type))];$('#pub-filters').innerHTML=['Tümü',...types].map((t,i)=>`<button class="filter ${i===0?'active':''}" data-type="${t==='Tümü'?'all':esc(t)}">${esc(t)}</button>`).join('');renderPubs(pubs);$$('.filter').forEach(b=>b.addEventListener('click',()=>{$$('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');renderPubs(b.dataset.type==='all'?pubs:pubs.filter(x=>x.type===b.dataset.type));}));}
function renderPubs(pubs){$('#pub-list').innerHTML=[...pubs].sort((a,b)=>b.year-a.year).map(p=>`<article class="pub"><div class="pub-year">${p.year}</div><div><span class="tag">${esc(p.type)}</span><h3>${esc(p.title)}</h3><p>${esc(p.venue)}</p>${p.doi?`<div class="doi">DOI: ${esc(p.doi)}</div>`:''}</div>${p.url?`<a class="pub-link" href="${esc(p.url)}" target="_blank" rel="noopener" aria-label="Yayını aç">↗</a>`:'<span></span>'}</article>`).join('') || '<div class="empty">Henüz kayıt yok.</div>'}
async function initResults(){mountChrome('results');const results=await loadJSON('./data/results.json');$('#results-grid').innerHTML=results.map(r=>`<article class="result-card"><span class="eyebrow">${esc(r.category)}</span><h3>${esc(r.title)}</h3><p>${esc(r.description)}</p>${r.links?.map(l=>`<a class="link-arrow" href="${esc(l.url)}" target="_blank" rel="noopener">${esc(l.label)} ↗</a>`).join('')||''}<div><span class="status">${esc(r.status)}</span></div></article>`).join('')}
document.addEventListener('DOMContentLoaded',()=>{const page=document.body.dataset.page;({home:initHome,news:initNews,history:initHistory,pubs:initPublications,results:initResults}[page]||(()=>mountChrome('')))().catch(err=>{console.error(err);const target=$('#page-error');if(target)target.textContent='İçerik yüklenemedi. GitHub Pages üzerinde çalıştırıldığından emin olun.';});});
